% !TeX program = xelatex
\documentclass[UTF8, zihao=-4, oneside]{ctexrep} % 推荐这种，全局设置正文为小四号字

% ====== 常用宏包 ======
\usepackage{graphicx}   % 插图
\usepackage{booktabs}    % 三线表
\usepackage{amsmath,amssymb} % 数学公式
\usepackage{geometry}    % 页边距
\usepackage{hyperref}    % 超链接（目录可点击）
\usepackage{cleveref}    % 智能交叉引用（可选）
\usepackage{bm}          % 粗体数学符号
\usepackage{mathtools}   % amsmath 增强版
\usepackage{setspace}
\usepackage{caption}
\usepackage{multirow}
\usepackage[table]{xcolor}
\usepackage{xparse}
\usepackage{tabularx}
\usepackage{xeCJK}
\xeCJKsetup{AutoFakeBold=true}
\usepackage[titles]{tocloft} % 使用 titles 选项以兼容 ctex 的标题样式

% 给 chapter 级别（包括摘要和各章大标题）增加引导点
\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}

% 核心：设置一级标题（摘要、Abstract、各章标题）之前的垂直间距
% 默认通常是 1.0em 甚至更多，改为 2pt 到 5pt 会变得非常紧凑
\setlength{\cftbeforechapskip}{3pt} 

\NewDocumentCommand{\safeimage}{O{\linewidth} m}{
  \IfFileExists{#2}{
    \includegraphics[width=#1]{#2}
  }{
    \fbox{
      \begin{minipage}[c][5cm][c]{#1}
        \centering
        图片暂缺\\[4pt]
        {\ttfamily\detokenize{#2}}
      \end{minipage}
    }
  }
}

% ====== 级别标题样式全局设置 ======
\ctexset{
    chapter = {
        format = {\centering\heiti\zihao{3}\bfseries}, % 一级标题：三号黑体加粗居中
        beforeskip = {1\baselineskip}, % 段前1行
        afterskip = {1\baselineskip},  % 段后1行
        fixskip = true                 % 强制间距生效
    },
    section = {
        format = {\heiti\zihao{4}\bfseries}, % 二级标题：四号黑体加粗
        beforeskip = {0.5\baselineskip},     % 段前0.5行
        afterskip = {0.5\baselineskip},      % 段后0.5行
        fixskip = true
    },
    subsection = {
        format = {\heiti\zihao{-4}\bfseries}, % 三级标题：小四黑体加粗
        beforeskip = {0.3\baselineskip},      % 段前0.3行
        afterskip = {0.3\baselineskip},       % 段后0.3行
        fixskip = true
    },
    subsubsection = {
        % 四级标题：缩进正文样式（2em）、加粗、1.25倍行距
        format = {\heiti\zihao{-4}\bfseries}, 
        indent = {2em},                        % 首行缩进两个字符
        beforeskip = {0pt},                    % 四级标题通常紧跟正文
        afterskip = {0pt},
        % 这里处理1.25倍行距：可以使用 \setstretch{1.25} 在具体章节内局部调整
    }
}

% 设置全局正文行距（通常学校要求是1.25或1.5倍，这里设为1.25）
\setstretch{1.25}


% ====== 页面设置（你后面可按学校要求改）======
\geometry{a4paper,left=3cm,right=2.5cm,top=2.8cm,bottom=2.8cm}

% ====== 超链接样式 ======
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  citecolor=blue
}

% ====== 论文信息 ======
\title{基于改进3DGS的虚实融合场景重建与应用研究}
\author{王伊硕}
\date{2026年3月}

% ====== 公式编号：显示为（3.1）=====
\numberwithin{equation}{chapter} % 保持 3.1, 3.2 逻辑

% 公式标签格式
\newtagform{ccnustyle}{（}{）}

% 启用这个格式
\usetagform{ccnustyle}

% 如果不想 \cref 自动显示“公式”，就不要设置 equation 的名字
% \crefname{equation}{公式}{公式}

\begin{document}

% ====== 封面 ======
\maketitle

% ====== 前置部分：罗马页码 ======
\clearpage
\pagenumbering{Roman}
\pagestyle{plain}

% ====== 中文摘要 ======
\include{chapters/abstract}

% ====== 英文摘要 ======
\include{chapters/abstract_en}

% ====== 目录：不显示页码 ======
\clearpage
\begingroup
  \pagestyle{empty}
  % 核心改进：临时让 \thispagestyle 失效，防止目录第一页被强制加上页码
  \renewcommand{\thispagestyle}[1]{} 
  
  \let\cleardoublepage\clearpage
  \tableofcontents
  
  % 关键：必须在 \endgroup 前使用 \clearpage，确保目录最后一页排版完后再恢复原本的样式
  \clearpage
\endgroup

% ====== 正文：阿拉伯页码 ======
\clearpage
\pagenumbering{arabic}
\pagestyle{plain}

\include{chapters/chap1}
\include{chapters/chap2}
\include{chapters/chap3}
\include{chapters/chap4}
\include{chapters/chap5}
\include{chapters/chap6}

% ====== 参考文献 ======
\bibliographystyle{plain}
\bibliography{refs}

\end{document}
